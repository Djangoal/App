name: 01 Build APK 2026

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: "Type de build"
        required: true
        default: "debug"
        type: choice
        options:
          - debug
          - release

jobs:
  build:
    runs-on: ubuntu-22.04

    env:
      SDK_DIR: ${{ github.workspace }}/android-sdk
      ANDROID_NDK_HOME: ${{ github.workspace }}/android-sdk/ndk/25.2.9519653

    steps:

    # ============================================================
    # CHECKOUT
    # ============================================================
    - name: Checkout code
      uses: actions/checkout@v5

    # ============================================================
    # PYTHON 3.11
    # ============================================================
    - name: Setup Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: "3.11"

    # ============================================================
    # JAVA 17
    # ============================================================
    - name: Setup Java 17
      uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: "17"

    # ============================================================
    # DEPENDANCES SYSTEME
    # ============================================================
    - name: Install system dependencies
      run: |
        sudo apt-get update

        sudo apt-get install -y \
          build-essential \
          libltdl-dev \
          zip \
          unzip \
          libncurses-dev \
          libtool \
          libtool-bin \
          automake \
          autoconf \
          pkg-config \
          m4 \
          curl

    # ============================================================
    # VERIFICATION JAVA
    # ============================================================
    - name: Verify Java
      run: |
        echo "========================================"
        echo "JAVA"
        echo "========================================"

        java -version
        javac -version

        echo "JAVA_HOME=$JAVA_HOME"

    # ============================================================
    # PYTHON-FOR-ANDROID
    # ============================================================
    - name: Setup python-for-android
      run: |
        python -m venv venv

        source venv/bin/activate

        echo "========================================"
        echo "MISE A JOUR PIP"
        echo "========================================"

        python -m pip install --upgrade pip
        python -m pip install --upgrade setuptools wheel

        echo "========================================"
        echo "INSTALL CYTHON"
        echo "========================================"

        pip install "Cython>=3.0,<3.1"

        echo "========================================"
        echo "INSTALL BUILD / VIRTUALENV"
        echo "========================================"

        pip install build virtualenv

        echo "========================================"
        echo "INSTALL PYTHON-FOR-ANDROID"
        echo "========================================"

        pip install "python-for-android==2026.05.09"

        echo "========================================"
        echo "VERSIONS"
        echo "========================================"

        python --version
        pip --version
        cython --version
        python -m build --version
        p4a --version

    # ============================================================
    # ANDROID SDK + NDK
    # ============================================================
    - name: Download Android SDK and NDK
      run: |
        mkdir -p "$SDK_DIR"

        curl -L \
          https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip \
          -o cmdline-tools.zip

        unzip -q cmdline-tools.zip -d "$SDK_DIR"

        mv "$SDK_DIR/cmdline-tools" "$SDK_DIR/cmdline-tools-temp"

        mkdir -p "$SDK_DIR/cmdline-tools/latest"

        mv "$SDK_DIR/cmdline-tools-temp"/* \
          "$SDK_DIR/cmdline-tools/latest/"

        export PATH="$SDK_DIR/cmdline-tools/latest/bin:$PATH"

        echo "========================================"
        echo "SDKMANAGER"
        echo "========================================"

        sdkmanager --version

        echo "========================================"
        echo "ANDROID LICENSES"
        echo "========================================"

        yes | sdkmanager \
          --sdk_root="$SDK_DIR" \
          --licenses || true

        echo "========================================"
        echo "INSTALLATION SDK / NDK"
        echo "========================================"

        sdkmanager \
          --sdk_root="$SDK_DIR" \
          "platform-tools" \
          "platforms;android-35" \
          "build-tools;35.0.0" \
          "ndk;25.2.9519653"

    # ============================================================
    # CONFIGURATION ANDROID
    # ============================================================
    - name: Configure Android environment
      run: |
        echo "ANDROID_SDK_ROOT=$SDK_DIR" >> "$GITHUB_ENV"
        echo "ANDROID_HOME=$SDK_DIR" >> "$GITHUB_ENV"
        echo "ANDROID_NDK_ROOT=$ANDROID_NDK_HOME" >> "$GITHUB_ENV"

        echo "$SDK_DIR/cmdline-tools/latest/bin" >> "$GITHUB_PATH"
        echo "$SDK_DIR/platform-tools" >> "$GITHUB_PATH"

        echo "========================================"
        echo "SDK"
        echo "$SDK_DIR"
        echo "========================================"

        echo "========================================"
        echo "NDK"
        echo "$ANDROID_NDK_HOME"
        echo "========================================"

    # ============================================================
    # NETTOYAGE COMPLET
    # ============================================================
    - name: Clean previous builds
      run: |
        echo "========================================"
        echo "NETTOYAGE"
        echo "========================================"

        rm -rf .buildozer
        rm -rf build
        rm -rf dist
        rm -rf bin

        rm -rf ~/.local/share/python-for-android
        rm -rf ~/.cache/pip

        source venv/bin/activate

        p4a clean_builds || true

    # ============================================================
    # VERIFICATION ENVIRONNEMENT
    # ============================================================
    - name: Verify build environment
      run: |
        source venv/bin/activate

        echo "========================================"
        echo "PYTHON"
        echo "========================================"

        python --version
        which python

        echo "========================================"
        echo "CYTHON"
        echo "========================================"

        cython --version
        which cython

        echo "========================================"
        echo "P4A"
        echo "========================================"

        p4a --version
        which p4a

        echo "========================================"
        echo "JAVA"
        echo "========================================"

        java -version

        echo "========================================"
        echo "ANDROID SDK"
        echo "========================================"

        test -d "$ANDROID_SDK_ROOT"
        echo "SDK OK"

        echo "========================================"
        echo "ANDROID NDK"
        echo "========================================"

        test -d "$ANDROID_NDK_HOME"
        echo "NDK OK"

        echo "========================================"
        echo "LLVM"
        echo "========================================"

        test -d \
          "$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64"

        echo "LLVM OK"

    # ============================================================
    # BUILD APK
    # ============================================================
    - name: Build APK
      run: |
        source venv/bin/activate

        BUILD="${{ github.event.inputs.build_type }}"

        echo "========================================"
        echo "BUILD TYPE : $BUILD"
        echo "========================================"

        if [ "$BUILD" = "debug" ]; then

          echo "========================================"
          echo "BUILD DEBUG"
          echo "========================================"

          p4a apk \
            --private "$GITHUB_WORKSPACE/app" \
            --package=org.example.monapp \
            --name="Mon budget perso" \
            --version=1.0 \
            --bootstrap=sdl2 \
            --icon="$GITHUB_WORKSPACE/logo.png" \
            --presplash="$GITHUB_WORKSPACE/logo1.png" \
            --requirements=python3==3.11.13,hostpython3==3.11.13,kivy \
            --permission INTERNET \
            --permission ACCESS_NETWORK_STATE \
            --orientation=portrait \
            --arch=arm64-v8a \
            --sdk_dir="$SDK_DIR" \
            --ndk_dir="$ANDROID_NDK_HOME" \
            --android_api=35 \
            --dist-name=monapp_debug \
            --debug
        elif [ "$BUILD" = "release" ]; then

          echo "========================================"
          echo "BUILD RELEASE"
          echo "========================================"

          p4a apk \
            --private "$GITHUB_WORKSPACE/app" \
            --package=org.example.monapp \
            --name="Mon budget perso" \
            --version=1.0 \
            --bootstrap=sdl2 \
            --icon="$GITHUB_WORKSPACE/logo.png" \
            --presplash="$GITHUB_WORKSPACE/logo1.png" \
            --requirements=python3==3.11.13,hostpython3==3.11.13,kivy \
            --permission INTERNET \
            --permission ACCESS_NETWORK_STATE \
            --orientation=portrait \
            --arch=arm64-v8a \
            --sdk_dir="$SDK_DIR" \
            --ndk_dir="$ANDROID_NDK_HOME" \
            --android_api=35 \
            --dist-name=monapp_release \
            --release

        else

          echo "❌ Type de build inconnu : $BUILD"
          exit 1

        fi

    # ============================================================
    # RECHERCHE APK
    # ============================================================
    - name: Find APK
      run: |
        echo "========================================"
        echo "APK GENERES"
        echo "========================================"

        find . -type f -name "*.apk" -print

    # ============================================================
    # VERIFICATION APK
    # ============================================================
    - name: Verify APK exists
      run: |
        APK=$(find . -type f -name "*.apk" | head -n 1)

        if [ -z "$APK" ]; then
          echo "❌ Aucun APK trouvé"
          exit 1
        fi

        echo "========================================"
        echo "APK TROUVE"
        echo "========================================"

        echo "$APK"
        ls -lh "$APK"

    # ============================================================
    # RENOMMAGE APK
    # ============================================================
    - name: Rename APK
      run: |
        APK=$(find . -type f -name "*.apk" | head -n 1)

        if [ -z "$APK" ]; then
          echo "❌ Aucun APK à renommer"
          exit 1
        fi

        BUILD="${{ github.event.inputs.build_type }}"

        if [ "$BUILD" = "debug" ]; then

          cp "$APK" mon_budget_perso_debug.apk

          echo "========================================"
          echo "APK DEBUG"
          echo "========================================"

        else

          cp "$APK" mon_budget_perso_release.apk

          echo "========================================"
          echo "APK RELEASE"
          echo "========================================"

        fi

        ls -lh *.apk

    # ============================================================
    # UPLOAD APK
    # ============================================================
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: mon-budget-${{ github.event.inputs.build_type }}
        path: "*.apk"
        if-no-files-found: error
