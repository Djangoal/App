- name: Build APK
      run: |
        source venv/bin/activate
        echo "Build type: ${{ github.event.inputs.build_type }}"

        if [ "${{ github.event.inputs.build_type }}" = "release" ]; then
          FINAL_APK="$GITHUB_WORKSPACE/mon_budget_perso_release_unsigned.apk"
          BUILD_TYPE="--release"
          DIST_NAME="monapp_release"
          ARCH="arm64-v8a"
        else
          FINAL_APK="$GITHUB_WORKSPACE/mon_budget_perso_debug.apk"
          BUILD_TYPE="--debug"
          DIST_NAME="monapp_debug"
          ARCH="armeabi-v7a"
        fi

        echo "=== Remplacement du manifest personnalisé ==="
        mkdir -p ~/.local/share/python-for-android/templates/
        cp app/templates/AndroidManifest.tmpl.xml ~/.local/share/python-for-android/templates/AndroidManifest.tmpl.xml

        echo "=== Extraction des classes.jar depuis les AAR ==="
        mkdir -p app/libs/jars
        for aar in app/libs/*.aar; do
          echo "→ Extraction de $aar"
          unzip -qo "$aar" classes.jar -d app/libs/jars/
        done

        echo "=== Préparation de la liste des jars à inclure ==="
        ADD_JARS=""
        for jar in $GITHUB_WORKSPACE/app/libs/jars/*.jar; do
          ADD_JARS="$ADD_JARS --add-jar=$jar"
        done

        echo "=== Compilation de l’APK avec AdMob ==="
        ./venv/bin/p4a apk \
          --private "$GITHUB_WORKSPACE/app" \
          --package=org.example.monapp \
          --name="Mon budget perso" \
          --version=0.1 \
          --bootstrap=sdl2 \
          --icon=logo.png \
          --presplash=app/logo1.png \
          --requirements=python3,kivy,android \
          --permission WRITE_EXTERNAL_STORAGE \
          --permission READ_EXTERNAL_STORAGE \
          --permission INTERNET \
          --permission ACCESS_NETWORK_STATE \
          --arch=$ARCH \
          --sdk_dir=$SDK_DIR \
          --ndk_dir=$ANDROID_NDK_HOME \
          --android_api=35 \
          --orientation=portrait \
          --dist-name=$DIST_NAME \
          $BUILD_TYPE \
          $ADD_JARS \
          --meta-data=com.google.android.gms.ads.APPLICATION_ID=ca-app-pub-3940256099942544~3347511713

        APK_PATH=$(find . -type f -name "*.apk" | head -n 1)

        if [ -z "$APK_PATH" ]; then
          echo "❌ Aucun APK trouvé !"
          find . -type f -name "*.apk"
          exit 1
        fi

        echo "✅ APK trouvé : $APK_PATH"
        cp "$APK_PATH" "$FINAL_APK"

        if [ "${{ github.event.inputs.build_type }}" = "release" ]; then
          $SDK_DIR/build-tools/35.0.0/zipalign -v 4 "$FINAL_APK" "${FINAL_APK%.apk}-aligned.apk"
          FINAL_APK="${FINAL_APK%.apk}-aligned.apk"
          echo "✅ APK aligné : $FINAL_APK"
        fi
